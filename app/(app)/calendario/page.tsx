import { Calendario } from "@/components/calendario/calendario";

// Calendario de eventos: por centro + globales, categorías con color, vista de mes. Handoff calendario-eventos.
export default function CalendarioPage() {
  return <Calendario />;
}
