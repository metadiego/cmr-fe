/**
 * Inicio: EN BLANCO, a propósito.
 *
 * Es el lienzo donde cada usuario armará su propia pantalla, como en el sistema anterior. No lleva
 * nada por defecto: ni un volcado de la sesión —correo, roles, identificadores— ni una redirección a
 * un módulo cualquiera. Suponer en qué trabaja cada persona y mandarla ahí es decidir por ella.
 *
 * Lo que antes vivía aquí (Apariencia, Mis tableros, Cerrar sesión) está en el menú del avatar, que
 * es donde la gente lo busca. Ver components/user-menu.tsx.
 */
export default function DashboardPage() {
  return <div className="min-h-[60vh]" aria-label="Inicio" />;
}
