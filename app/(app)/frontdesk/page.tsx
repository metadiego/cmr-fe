import { FrontdeskBoard } from "@/components/frontdesk/frontdesk-board";

// Frontdesk del día (F4): tabs por servicio + flujo de sesiones. Ver docs/plans/fe-frontdesk-dia.md.
export default function FrontdeskPage() {
  return <FrontdeskBoard />;
}
