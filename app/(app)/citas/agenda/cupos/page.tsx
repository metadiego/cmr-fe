import { AgendaConfig } from "@/components/agenda/agenda-config";

// Config hub for Citas Médicas: hourly capacity (cupos) + holidays (festivos).
export default function AgendaConfigPage() {
  return <AgendaConfig />;
}
