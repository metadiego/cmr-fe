import { RecepcionDesdeFactura } from "@/components/inventario/recepcion-desde-factura";

// Recibir compra desde el papel del proveedor (pegar → emparejar → confirmar). Handoff recepcion-desde-factura.
export default function RecepcionFacturaPage() {
  return <RecepcionDesdeFactura />;
}
