# i18n — Multi-language (Spanish + English) — Plan

**Date:** 2026-06-03
**Status:** Done

Spec: `docs/specs/2026-06-03-i18n-design.md`. Module doc: `docs/modules/i18n.md`.

## Files created
- `i18n/config.ts` — `locales`, `defaultLocale='es'`, `LOCALE_COOKIE`, `isLocale()`, `type Locale`.
- `i18n/request.ts` — `getRequestConfig` (async cookies → locale + messages + `timeZone` + `formats`).
- `i18n/locale-actions.ts` — `"use server" setLocale(locale)` (writes `NEXT_LOCALE`, 1y, lax).
- `messages/es.json`, `messages/en.json` — dictionaries (keys in English; `es` holds the original text).
- `components/language-toggle.tsx` — cycles locales → `setLocale` + `router.refresh()`.
- `docs/specs/2026-06-03-i18n-design.md`, `docs/modules/i18n.md`, `docs/modules/_TEMPLATE.md`.

## Files modified
- `next.config.ts` — wrapped with `createNextIntlPlugin('./i18n/request.ts')`.
- `app/layout.tsx` — async; `<html lang={await getLocale()}>`; `<NextIntlClientProvider>`.
- `lib/nav.ts` — `NavItem.label` → `labelKey` (`home`/`clients`/`appointments`).
- Migrated to `t()`/`getTranslations`: `app/page.tsx`, `app/login/page.tsx`, `app/login/actions.ts`,
  `app/(app)/dashboard/page.tsx`, `components/site-header.tsx` (+ mounts `LanguageToggle`),
  `components/search-bar.tsx` (now `"use client"`), `components/mode-toggle.tsx`,
  `components/api-health-check.tsx`.
- **`proxy.ts` not touched** (cookie mode needs no middleware).

## Verification (done)
- `typecheck` + `lint` + `build` green.
- dev :8080 — no cookie → `<html lang="es">` + Spanish; `NEXT_LOCALE=en` → `<html lang="en">` +
  English (landing + login confirmed via curl). `/dashboard` without session → 307 → `/login`
  (gating intact).

## Follow-ups
- `// TODO #51`: resolve locale by layered precedence (master override → user prefs via BE → cookie).
- Dashboard `Row` keys left as raw API field names (debug view), not translated.
