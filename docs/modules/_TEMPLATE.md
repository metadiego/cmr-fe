# Module: <name>

> Per-module documentation. One file per module/feature under `docs/modules/`.
> Copy this template when adding a module. Pair it with a dated spec+plan in
> `docs/specs/` and `docs/plans/`.

## Purpose
What this module does and why it exists (the need it addresses).

## Files
- `path/to/file.ts` — one-line role.

## i18n keys (namespaces)
Which `messages/*.json` namespace(s) this module owns. New strings are authored with **English
keys** (see `docs/modules/i18n.md`).

## How to use
Public API / components / hooks and how to call them.

## BE dependencies
Endpoints consumed via `lib/api/` (API-First). Required headers, tenant scoping, auth mode.

## Decisions
Notable choices and trade-offs (why this approach).

## Pending
Known gaps, TODOs, follow-ups.
